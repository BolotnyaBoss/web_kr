<h1>Articles</h1>
<div>
  <%= link_to "New Article", new_article_path %>
</div>
<br>
Our blog has <%= Article.count %> articles, like:
(<%= Article.public_count %> public
<%= Article.private_count %> private
<%= Article.archived_count %> archived)
<br>

<ul>
  <% @articles.each do |article| %>

    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>
<div>
  <%= link_to 'Previous page', root_path(page: @page - 1) %>
  <%= link_to 'Next page', root_path(page: @page + 1) %>
</div>

